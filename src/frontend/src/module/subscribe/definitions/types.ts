export enum SubscriptionType
{
    Theme = 1,
    Profile = 2,
    Collection = 3,
    Community = 4,
}

export enum SubscriptionTypeStr
{
    Theme = <any>"theme",
    Profile = <any>"profile",
    Collection = <any>"collection",
    Community = <any>"community",
}