article.component.component-profile-card-header
  .profile-image
    .profile-image-container
      cass-profile-image(border="circle", [url]="getImageURL()")
  .profile-about
      .profile-name
        h1 {{ getProfileName() }}
      .profile-settings
        time(*ngIf="hasTime()")
          span {{ getTime() }}
