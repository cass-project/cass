error:
  description: Error
  schema:
    type: object
    required:
      - success
    properties:
      success:
        type: boolean
        default: false
      error:
        type: string
        description: |
          Текст ошибки