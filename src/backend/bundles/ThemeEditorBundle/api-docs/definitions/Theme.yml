Data_Entity_Theme:
  type: object
  required:
    - id
    - parent_id
    - host
    - position
    - title
  properties:
    id:
      type: number
      description: ID
    parent_id:
      type: number
      description: ID родительской тематики
    host:
      type: object
      $ref: '#/definitions/Data_Entity_Host'
    position:
      type: number
      description: Позиция в текущем уровне дерева тематик
    title:
      type: string
      description: Название