'/backend/api/protected/post/update':
  post:
    tags:
      - post
    description: |
      Обновляет данные о тематике
    parameters:
      - name: body
        in: body
        required: true
        description: JSON Request
        schema:
          $ref: '#/definitions/Update_Post_Request'
    responses:
      '200':
        description: |
          Succesful response
        schema:
          type: object
          required:
            - id
            - success
            - entity
          properties:
            id:
              type: number
              description: ID созданной темы
            success:
              type: boolean
              default: true
              description: "= true"
            entity:
              $ref: '#/definitions/Data_Entity_Post'
      '400':
          $ref: '#/responses/error'
      '404':
        description: |
          Тематика не найдена
        schema:
          type: object
          required:
            - success
            - error
          properties:
            success:
              type: boolean
              description: "= false"
            error:
              type: string
              description: Сообщение об ошибке