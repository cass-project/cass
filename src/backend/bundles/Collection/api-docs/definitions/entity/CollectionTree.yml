Collection_Entity_Collection_Tree:
  type: object
  description: Коллекция.
  required:
    - id
    - parent_id
    - theme_id
    - title
    - description
    - position
    - children
    - depth
  properties:
    id:
      type: number
      description: ID
    parent_id:
      type: number
      description: ID родительской коллекции. Может содержать значение NULL.
    theme_id:
      type: number
      description: Тематика, которой принадлежит данная коллекция. Может содержать значение NULL.
    title:
      type: string
      description: Название (заголовок) коллекции
    description:
      type: string
      description: Описание коллекции
    position:
      type: number
      description: Позиция в текущем дереве
    depth:
      type: number
      description: Глубина вложенности
    children:
      schema:
        $ref: '#/definitions/Collection_Entity_Collection_Tree'