"/protected/collection/entity/create/for-profile/{profileId}":
  put:
    tags:
      - collection
    description:
      Создает новую коллекцию для указанного профиля
    parameters:
      - name: profileId
        in: path
        required: true
        type: number
        description:
          ID профиля (владельца коллекции)
      - name: body
        in: body
        required: true
        description: JSON Request
        schema:
          $ref: '#/definitions/Collection_Entity_Request'