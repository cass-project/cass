/protected/post/create:
  put:
    tags:
      - post
    description: |
      Создаёт новый пост
    parameters:
      - name: body
        in: body
        required: true
        description: JSON Request
        schema:
          $ref: '#/definitions/PUT_Post_Request'
    responses:
      '400':
          $ref: '#/responses/error'
      '200':
        description: |
          Successfull response
        schema:
          type: object
          required:
            - id
            - success
            - entity
          properties:
            id:
              type: number
              description: ID созданной темы
            success:
              type: boolean
              default: true
              description: "= true"
            entity:
              $ref: '#/definitions/Data_Entity_Post'