/protected/channel/create:
  put:
    tags:
      - channel
    description: |
      Создает новый канал
    parameters:
      - name: body
        in: body
        required: true
        description: JSON Request
        schema:
          $ref: '#/definitions/PUT_Channel_Request'
    responses:
      '400':
          $ref: '#/responses/error'
      '200':
        description: |
          Successfull response
        schema:
          type: object
          required:
            - id
            - success
            - entity
          properties:
            id:
              type: number
              description: ID создание канала
            success:
              type: boolean
              default: true
              description: "= true"
            entity:
              $ref: '#/definitions/Data_Entity_Channel'