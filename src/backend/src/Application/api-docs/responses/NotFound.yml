NotFound:
  description: 404 Not Found
  schema:
    required:
      - success
      - error
    properties:
      success:
        type: boolean
        description: '=false'
        default: false
      error:
        type: string
