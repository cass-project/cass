Unauthorized:
  description: 403 Not authorized
  schema:
    required:
      - success
      - error
    properties:
      success:
        type: boolean
        default: false
      error:
        type: string
        default: "No API Key available"
