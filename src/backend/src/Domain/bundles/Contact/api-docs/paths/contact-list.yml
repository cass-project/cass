"/protected/profile/{profileId}/contact/list":
  get:
    tags:
      - contact
    description:
      Список контактов профиля
    security:
      - x_api_key: []
    parameters:
      - name: profileId
        in: path
        required: true
        description: ID профиля
        type: number
    responses:
      403:
        schema:
          $ref: '#/responses/Unauthorized'
      404:
        description: Профиль не найден
        schema:
          $ref: '#/responses/NotFound'
      200:
        schema:
          required:
            - success
            - entity
          properties:
            success:
              type: boolean
              description: '=true'
            entities:
              type: array
              items:
                $ref: '#/definitions/Contact_Entity_Contact'