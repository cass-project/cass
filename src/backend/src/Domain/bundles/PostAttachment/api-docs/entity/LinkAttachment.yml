PostAttachment_PostAttachment_Link:
  required:
    - url
    - resource
    - metadata
  properties:
    url:
      type: string
      description: URL
    resource:
      type: string
      description: |
        Тип ресурса. Значения: "link", "youtube", "image"
    metadata:
      type: object
      description: Метаданные