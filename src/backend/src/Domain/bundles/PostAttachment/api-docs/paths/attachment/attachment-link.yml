"/protected/post-attachment/link":
  put:
    tags:
      - post_attachment
    description: Создает и возвращает PostAttachment (ссылка)
    parameters:
      - name: url
        description: URL
        type: string
        required: true
        in: query
    security:
      - x_api_key: []
    responses:
      403:
        $ref: '#/responses/Unauthorized'
      200:
        schema:
          required:
            - success
            - entity
          properties:
            success:
              type: boolean
              description: '=true'
            entity:
              $ref: '#/definitions/PostAttachment_PostAttachment_Link'