Avatar_ImageCollection:
  type: object
  required:
    - uid
    - is_auto_generated
    - variants
  properties:
    uid:
      type: string
      description: UID коллекции
    is_autogenerated:
      type: boolean
      description: Является автосгенерированной
    variants:
      type: object
      required:
        - default
      properties:
        default:
          $ref: '#/definitions/Avatar_Image'
        16:
          $ref: '#/definitions/Avatar_Image'
        32:
          $ref: '#/definitions/Avatar_Image'
        64:
          $ref: '#/definitions/Avatar_Image'
        128:
          $ref: '#/definitions/Avatar_Image'
        256:
          $ref: '#/definitions/Avatar_Image'
        512:
          $ref: '#/definitions/Avatar_Image'
