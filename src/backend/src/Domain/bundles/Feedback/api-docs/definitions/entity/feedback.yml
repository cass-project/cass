Feedback_Entity_Feedback:
  type: object
  required:
    - id
    - created_at
    - profile
    - type_feedback
    - description
  properties:
    id:
      type: number
      description: ID
    profile:
      type: object
      description: Информация об авторе фидбэка
      schema:
        required:
          - has
          - entity
        properties:
          has:
            type: boolean
            description: Если FALSE, то фидбэк анонимный
          entity:
            $ref: '#/definitions/Profile_Entity_Profile'
    created_at:
      type: string
      description: Дата создания
    type_feedback:
      type: number
      description: Тип фидбека
    description:
      type: string
      description: Текст описание фидбека
    responses:
      type: array
      items:
        schema:
          $ref: '#/definitions/Feedback_Entity_Feedback'