'/feedback/{feedbackId}/has-answer':
  get:
    tags:
      - feedback
    description: Получить ответы на фидбэк
    parameters:
      - name: feedbackId
        in: path
        required: true
        description: id feedback
        type: numeric
    responses:
      200:
        schema:
          properties:
            success:
              type: boolean
              default: true
            entities:
              type: array
              items:
                schema:
                  $ref: '#/definitions/Feedback_Entity_FeedbackResponse'
          required:
            - success
            - entities
