'/feedback/{feedbackId}/cancel':
  delete:
    tags:
      - feedback
    description: Удалить фидбэк
    parameters:
      - name: feedbackId
        in: path
        required: true
        description: ID фидбека
        type: numeric
    responses:
      200:
        schema:
          properties:
            success:
              type: boolean
              default: true
          required:
            - success
