"/protected/theme/create":
  put:
    tags:
      - theme
    description: |
      Создает новую тематику
    parameters:
      - name: body
        in: body
        required: true
        schema:
          $ref: '#/definitions/PUT_Theme_Request'
    responses:
      403:
        schema:
          $ref: '#/responses/Unauthorized'
      409:
        description: Дубликат тематики с указанным ID
        schema:
          $ref: '#/responses/error'
      200:
        description: |
          Successfull response
        schema:
          type: object
          required:
            - id
            - success
            - entity
          properties:
            success:
              type: boolean
              description: "= true"
            entity:
              $ref: '#/definitions/Theme_Entity_Theme'