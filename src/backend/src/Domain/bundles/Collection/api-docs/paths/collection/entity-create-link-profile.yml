"/protected/profile/{profileId}/collection/create":
  put:
    tags:
      - collection
    description:
      Создает новую коллекцию для указанного профиля
    parameters:
      - name: profileId
        in: path
        required: true
        type: number
        description:
          ID профиля
      - name: body
        in: body
        required: true
        description: JSON Request
        schema:
          $ref: '#/definitions/Collection_Entity_Request'
    responses:
      200:
        description: Коллекция создана
        schema:
          type: object
          required:
            - success
            - entity
          success:
            type: boolean
            description: '=true'
          entity:
            $ref: "#/definitions/Collection_Entity_Collection"