Feed_Request_FeedRequest:
  required:
    - criteria
  properties:
    criteria:
      type: array
      items:
        type: object
        schema:
          required:
            - code
            - params
          properties:
            code:
              type: string
              description: Код критерия
            params:
              type: object
              description: Параметры критерия