"/feed/collection/{collectionId}":
  post:
    tags:
      - feed
    description:
      Получить список сообщений в коллекции
    parameters:
      - name: collectionId
        in: path
        required: true
        type: number
        description: ID коллекции
      - name: body
        in: body
        required: true
        description:
        schema:
          $ref: '#/definitions/Feed_Criteria_Request'
    responses:
      200:
        schema:
          $ref: '#/definitions/Feed_ResultSet'