/protected/profile/{profileId}/greetings-as/fl/:
  post:
    tags:
      - profile
    description: Назвать пользователя по имени и фамилии. Также отмечает профиль как проинициализированый.
    security:
      - x_api_key: []
    parameters:
      - name: profileId
        type: number
        in: path
        required: true
        description: ID профиля
      - name: body
        in: body
        required: true
        description: Профиль
        schema:
          $ref: '#/definitions/Profile_GreetingsAsFL_Request'
    responses:
      200:
        schema:
          $ref: '#/responses/success'
      403:
        $ref: '#/responses/Unauthorized'
